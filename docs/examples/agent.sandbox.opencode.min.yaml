# 极简沙箱 Agent 模板 — 等价于 agent.harness.yaml.example 的最小可部署子集。
# 完整字段与中文说明见仓库根：agent.harness.yaml.example
#
#   cp agent.harness.yaml.example agent.harness.yaml
#   或 cp 本文件 → agent.harness.yaml 后按需删减

name: My Sandbox Agent
runtime: harness
engine: opencode
system: |
  You are a helpful coding assistant in a remote sandbox workspace.
  Reply concisely.

metadata:
  runtime: open-managed-agent
  harness: "1"
