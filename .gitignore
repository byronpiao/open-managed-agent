.DS_Store
node_modules/
dist/
*.js.map
.env
.env.harness
!.env.example
!.env.harness.example
!.env.*.example
scripts/harness/scenarios/.env.*
!scripts/harness/scenarios/.env.*.example
agent.yaml
agent.harness.yaml
agent.harness.claude.yaml
!agent.yaml.example
!agent.harness.yaml.example
.plan/
# Prevent accidentally committing agent.yaml from agent-runtime package dir
# (it overrides AGENT_CONFIG_B64 and breaks cloud deploys)
packages/agent-runtime/agent.yaml
packages/agent-runtime/agent.yml
packages/agent-runtime/agent.harness.yaml
# Legacy local deploy copy — use agent.harness.yaml
agent.sandbox.yaml

# CocoIndex Code (ccc) — local semantic index
.cocoindex_code/

# CodeGraph — local AST graph (*.db ignored inside .codegraph/.gitignore)
.codegraph/

# Cursor — local IDE config (not shared)
.cursor/
