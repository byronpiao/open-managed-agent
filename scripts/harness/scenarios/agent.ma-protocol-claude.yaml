# Deployed harness Runtime for Managed Agents HTTP acceptance (claude engine).
# magent agent:update -f scripts/harness/scenarios/agent.ma-protocol-claude.yaml -i <agent-id>
name: Harness MA Protocol Claude
runtime: harness
engine: claude
sandbox:
  infra: serverless
system: |
  Reply concisely for Managed Agents HTTP acceptance.

metadata:
  runtime: open-managed-agent
  harness: "1"
  acceptance: ma-protocol-claude
