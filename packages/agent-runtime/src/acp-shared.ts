/**
 * ACP wire helpers — re-exported from the shared runtime-agnostic module.
 *
 * Kept as a local file so the harness tree's relative imports keep resolving
 * unchanged. Logic lives only in open-managed-agent-runtime-shared/acp-shared.js.
 */
export * from "open-managed-agent-runtime-shared/acp-shared.js";
